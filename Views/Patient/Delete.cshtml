@model HospitalSystem.ViewModels.PatientViewModel
@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="card-title">Delete Patient</h2>

<div class="col-10 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Paients</h4>
            <form class="forms-sample">

                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
             

                <dl class="dl-horizontal">
                

                    <dt>
                        @Html.DisplayNameFor(model => model.PatientName)
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.PatientName)
                    </dd>
                </dl>
                    <button type="button" class="btn btn-primary me-2" onclick="DeletePatient()">Submit</button>
                    <button class="btn btn-light">Cancel</button>
            </form>
        </div>
    </div>
</div>

<div>
    @Html.ActionLink("Back to List", "AllPatients")
</div>